"""
WebSocket URL routing configuration
"""

from django.urls import re_path

# WebSocket URL patterns
ws_urlpatterns = [
    # Add WebSocket routes here as needed
    # Example: re_path(r'ws/chat/(?P<room_name>\w+)/$', consumers.ChatConsumer.as_asgi()),
]
